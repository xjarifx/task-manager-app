For the sake on simple city, i didn't used:
    1. user login functionality (yes, everyone who access the site will see the same list of tasks)
    2. uuid/time based id generation
    3. multi file/folder (like separate file and folder for database/controller/service etc.)
    4. 